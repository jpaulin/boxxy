<?php
	// DB configuration items like database name
	global $dbName = 'BOOKBASE';
	// DB security credentials configuration
	global $dbUser = undefined;

    /*
     * Frontend and backend for Books (aka. 'boxxy')
     * See: README.md 
     */

    - how many different types of queries?
      - client is JavaScript Frontend
      - think in JavaScript

    - do code for endpoints for REST
      - capture a call 
      - waits for incoming TCP
      - parses the URL 
      - gets header -embedded parameters which the Client (JS) has sent
      - what is the "method" in RFC 


    $sqlq = "SELECT * from $ ";

    /** 
     * 
     * Initialize connection to mysql database */

    private function initDatabase ($value='')
    {
    	return;
    }



?>
